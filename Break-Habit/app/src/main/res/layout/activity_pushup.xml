<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_pushup"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="spookylobster.break_habit.PushupActivity"
    android:orientation="vertical">

    <TextView
        android:text="Do push-ups and press your chin on the screen"
        android:textSize="28dp"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:id="@+id/pushup_title"
        android:layout_weight="1"
        android:gravity="center"
        android:textAlignment="center" />

    <TextView
    android:text="TextView"
    android:textSize="64dp"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:id="@+id/pushup_counter"
    android:layout_weight="0.3"
    android:gravity="center"
    android:textAlignment="center"
    android:background="@mipmap/pushup_background"/>

</LinearLayout>
